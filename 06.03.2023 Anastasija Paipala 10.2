#include <iostream>
#include <vector>
using namespace std;
double sum()
{
	double num1, num2;
	cout << "ievadi 2 sk" << endl;
	cin >> num1 >> num2;
	return num1 + num2;
}
double quard()
{
	double num1;
	cout << "ievadi 1 sk" << endl;
	cin >> num1;
	return num1 * num1;
}
void average()
{
	vector<double> n { 1, 2, 3, 4, 5 };
	cout << (n[0] + n[1] + n[2] + n[3] + n[4]) / 5;
}
void even() {
	vector<int> n{ 1,2,3,4,5 };
	int i = 0;
	do {
		if (n[i] % 2 == 0)cout << n[i] << " ";
		i++;
	} while (i < n.size());

}
void prime()
{
	vector <int> p {2, 3, 7, 11, 13, 17, 19, };
	int num1;
	cout << "ievadi 1 sk zem 20 kas ir pozitivs" << endl;
	cin >> num1;
	int i = 0; bool a = false;
	do {
		if (num1 == p[i]) { cout << "true"; a = true; }

		i++;
	} while (i < p.size());
	if (a == false)cout << "false";


}
void bg()
{
	vector <int> bg{ 1,2,3 };
	int i = 0, sum = 0;
	do {
		cout << bg[i] * bg[i] << " ";
		sum = sum + bg[i] * bg[i];
        i++;
	} while (i < bg.size());

	cout << sum;
}
void poz()
{
	vector <int> pozit{ 1,-2,3,-4,5 };

	int i = 0;
	do {
		if (pozit[i] > 0) cout << pozit[i] << " ";
		i++;
	} while (i < pozit.size());
}
void negat()
{
	vector <int> negat{ 1,-2,3,-4,5 };

	int i = 0;
	do {
		if (negat[i] < 0) cout << negat[i] << " ";
		i++;
	} while (i > negat.size());
}

void vards()
{
	string var = "hello";
	int i = var.size();
	do {
		cout << var[i];
		i--;
	} while (i !=-1);
}
void polindrom()
{
	string pol = "racecar";
	if (pol[0] == pol[6] && pol[1] == pol[5] && pol[2] == pol[4]);
}
vector<int> a(vector<int>k)
{
	return k;
}
double difference()
{
	double num1, num2;
	cout << "ievadi 2 sk" << endl;
	cin >> num1 >> num2;
	return num1 - num2;
}
double quard1()
{
	double num1;
	cout << "ievadi 1 sk" << endl;
	cin >> num1;
	return num1 * num1 * num1;
}
void divkarsota()
{
	double num1;
    cout << "ievadi 1 sk" << endl;
    cin >> num1;
    cout << num1 * 2;
}
void even1() {
	vector<int> n{ 1,2,3,4,5,6,7,8 };
	int i = 0;
	do {
		if (n[i] % 2 != 0)cout << n[i] << " ";
		i++;
	} while (i < n.size());

}
void even2()
{
	int num1;
	cout << "ievadi para skaitli"<< endl;
	cin >> num1;
	if (num1 % 2 == 0)
	{
		cout << "true";
	}
	if (num1 % 2 != 0);
	{
		cout << "false";
	}
} void vards()
{
	string vards = "ammendment";
	cout << vards[0] << vards[1] << vards[2] << vards[3] << vards[4];
}

int main()
{
	/*vector<int> k{ 1,2,3 };
	cout << a(k)[0] << a(k)[1] << a(k)[2];*/
	//cout << sum()<<" "<<quard();
	//average();
	//even();
	//prime();
	//bg();
	//poz();
	//negat();
	//vards();
	//polindrom();
	//cout << difference();
	//cout << quard1();
	//divkarsota();
	//even1();
	//even2();
	//vards();


}
